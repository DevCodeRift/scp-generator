# SvelteKit adapter-node body size limit (disabled - upload endpoint handles its own limit via streaming)
BODY_SIZE_LIMIT=Infinity

# Temp directory for video processing
VIDEO_TEMP_DIR=./tmp/video-processing
